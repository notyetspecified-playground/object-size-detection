## Object size detection using OpenCV and ArUco Markers

### Install dependencies
``` shell
sudo apt install python3 ffmpeg libsm6 libxext6
```
``` shell
python3 -m pip install opencv-contrib-python
```
